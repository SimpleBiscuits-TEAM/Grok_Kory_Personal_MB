{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "dev: localhost (server + Vite)",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "windows": {
        "options": {
          "env": {
            "PATH": "${env:ProgramFiles}\\nodejs;${env:LOCALAPPDATA}\\Programs\\node;${env:PATH}"
          }
        }
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "focus": true
      },
      "runOptions": {
        "instanceLimit": 1
      }
    },
    {
      "label": "git-map: HTML-Grafik erzeugen",
      "type": "shell",
      "command": "npm run git-map",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "git-map: Watch (Grafik bei Git-Änderung)",
      "type": "shell",
      "command": "npm run git-map:watch",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "isBackground": true,
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated"
      }
    }
  ]
}

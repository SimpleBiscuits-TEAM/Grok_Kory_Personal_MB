PDF Health Report Visual Inspection - Key Finding:

Page 7 contains the garbled text. In the "ADVANCED TUNING ANALYSIS" section:
"Timing peaked at 25.0° BTDC, averaging 11.6°. Getting into the performance range but still within reason. High timing makes power but also makes cylinder pressure — it's a balancing act. &  Low timing at high pulse width: 25.0° at 2.34ms PW and 3378 RPM."

The "&" character appears to be a stray ampersand from HTML entity encoding (&amp;) that wasn't properly decoded, or it's a formatting artifact where a bullet point or line break was converted to "&".

This text comes from the LLM-generated reasoning/analysis. The issue is likely in:
1. The LLM response containing HTML entities or markdown that isn't being properly sanitized
2. The PDF generator not properly escaping/unescaping HTML entities in the text content

Need to check: healthReportPdf.ts and the reasoning engine output sanitization.
